[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino

monitor_speed = 115200
upload_speed = 921600

build_flags =
    -D USER_SETUP_LOADED=1
    -D ILI9341_2_DRIVER
    -include include/User_Setup.h

lib_deps =
    bodmer/TFT_eSPI